module GDMath {
    export interface IMathOps {
        add(x: number, y: number) : number;
        substract(x: number, y: number) : number;
        multiply(x: number, y: number) : number;
        divide(x: number, y: number) : number;
    }
}